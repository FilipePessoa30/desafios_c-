//Exercício 6: Filtragem in-place com referência (vector + loop + condicional)
//Implemente:
//int removerIntervalo(vector<int>& v, int lo, int hi)
//Remove do vetor todos os valores x com lo <= x <= hi, e retorna quantos foram removidos.
///Dica: crie um novo vector<int> out e depois v = out (mais simples), ou faça in-place com dois índices.